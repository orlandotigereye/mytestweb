--- Private Project Memory Index (private, not committed to repo) ---
--- Context from: C:/Users/user/.gemini/tmp/25060301/memory/MEMORY.md ---
--- Private Project Memory Index (private, not committed to repo) ---
## Gemini Added Memories

- strokeAPI: "https://script.google.com/macros/s/AKfycbyHr7Sj-UVAb_-j57J9p8aIxij1xGPVTHoYmh8umNbAW2W5dFw51dLH1HboWYbKHDkCLw/exec"
- 使用者偏好：所有前後台訊息提示都不使用 alert。
- The user wants me to answer in both English and Traditional Chinese, separated by "---".
- 程式碼生成規則: 1. 不得編造任何不存在的 API、函式、套件或網址。 2. 只使用官方已存在的標準語法與套件。 3. 若資訊不足，請回覆「此處需要你提供資料」。 4. 程式碼必須可直接執行，不可使用偽代碼。 5. 所有外部依賴必須明確列出版本。
- All .html development should adopt RWD (Responsive Web Design) mode, ensuring it does not exceed screen width.
- Every program should include a button to download its own source code.
- Every template should use a tabbed interface (分頁模式) and use a small font size (小字模式).
- HTML code content definition order: 1. CSS, 2. Fixed Data, 3. Script, 4. HTML.
- Always use new files (with incremented letter in filename) instead of `replace` for code modifications or debugging.
- When using the `read_file` tool, for "offset 4000 lines" this means to use `start_line=1` and `end_line=4000`.
- The user wants Notion documentation saved in 'public/02.Notion/' and Gumroad documentation saved in 'public/01.GumRoad/'.
- Knowledge-related content (知識類) should be placed in 'public/03.Know/'.
- Record daily user questions in English and Chinese, saved in 'public/03.Know/01.Day/' with the naming convention 'YYYYMMDD_Serial.txt'.
- The user wants Notion documentation saved in 'public/02.Notion/', Gumroad documentation in 'public/01.GumRoad/', SOP files in 'public/06.SOP/', and ZIP archives in 'public/07.ZIP/'.
- The user wants MP4 video files saved in 'public/08.MP4/'.
- The project follows "Royal Gold Unified Standards" for all new HTML applications:
1. CSS: 'public/00CSS/global.css' must be inlined for Design Tokens (Colors, Spacing, Small fonts 13px).
2. JS: 'public/01JS/core.js' must be inlined for common utilities (Firebase, Toast, Spinner, Sync, STT, DownloadSource).
3. Structure: Always use the Tabbed Interface App Shell (tabs-container, content-section) from 'public/0C.Template/AppShellN.html'.
4. Logic: Use APP_CORE methods (showToast, toggleSpinner, getLocal, saveLocal, syncToFirebase, downloadSelf).
5. No alert popups allowed; use APP_CORE.showToast instead.
6. Naming: Increment filename letter (e.g., AppA.html -> AppB.html) for new versions.
- The user wants all software development tasks to follow the 'Royal Gold Unified Standards' (Dynamic Pathing with path_helper.js, 4-Layer Code Structure, App Shell Componentization, 13px Small Font mode) unless stated otherwise. However, items in the 'public/APK' directory are exempt from these standards.

- 新增,或修改程式,一律用新建立程式,不要用取代的方法

- **Backend Message Display:** All backend messages must also be displayed on the frontend, utilizing a dedicated tab for backend information.
- **Unspecified files location:** `c:	mp2\web\25060301\public\0R.Other`
- **Video files location:** `c:	mp2\web\25060301\public\08.MP4`
- **String Concatenation with '+':** When concatenating strings using the `+` operator, keep the entire operation on a single line, unless it is within a loop.
- **String Concatenation in downloadSelf:** The `var content = "<!DOCTYPE html>"` must be on a single line without any newline characters (e.g., `
`) within the string literal itself, like this: `var content = "<!DOCTYPE html>" + document.documentElement.outerHTML;`
- **Avoid Syntax Errors in Future HTML Apps:** When generating new HTML applications, prioritize using `addEventListener` for dynamic event handling instead of inline `onclick`. Always use template literals (backticks) for robust and safer dynamic string construction, especially within `innerHTML` assignments, to prevent issues with string escaping and literal newlines.
- **Strict File Modification Policy:** For all code modifications, debugging, or fixes, *always create a new file* (following an incrementing naming convention like `AppA.html` -> `AppB.html` or `AppR.html` -> `AppS.html`). **Never use the `replace` tool for in-place modifications to existing files.**
- **Comment Style Policy:** All comments in generated or modified code must strictly use single-line JavaScript comment syntax (`//`). Block comments (`/* ... */`) or HTML comments (`<!-- ... -->`) are prohibited within script blocks. HTML comments at the very beginning of files (like metadata blocks) should be removed and converted to single-line `//` comments inside the first relevant script block.