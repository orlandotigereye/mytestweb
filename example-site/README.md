# Example Site

This is an example subsite demonstrating a Netlify Function call. Open `example-site/index.html` after deploying (or visit `https://<your-site>/.netlify/functions/example` to call the function directly).
